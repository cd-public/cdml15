function cs = findchanges(seq,parts)
len = size(seq,2)
cs = zeros(iter,len-1);
for i=1:10
    [T,E] = newstart(parts);
    [T,E] = hmmtrain(seq,T,E);
    tmp = hmmviterbi(seq,T,E);
    cs(i,:) = tmp(1:len-1);
    cs(i,:) = cs(i,:) - tmp(2:len);
end
% Make signs consistent
% Only useful if similar
% Otherwise no impact
